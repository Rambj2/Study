Steps to Push code to GitHub
cd D:\Maven\DropitAdmin-->Goto folder location then open CMD
git init
git remote add origin https://github.com/YourUsername/DropitAdmin.git
git commit -m "Added notes.txt"
git branch -M main
git push -u origin main

===============================================
If Code pushed to git first time then needs three commands only
git add .
git commit -m "Updated files from office"
git push

After making any changes by others on the same project then needs to run these commands only
cd YourRepo
git pull


Pom.xml
organilze all the data 
ctrl+A
ctrl+I
